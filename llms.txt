# AI-HIL

AI-HIL is a local MCP stdio server for AI agents working with embedded hardware.

Install the `aihil` command and MCP server once on the local machine:

```bash
npm i -g hp-8472/aihil
```

From a repository checkout, use `npm install --global .`.

Each firmware project should contain its own `.aihil/` directory with `.aihil/config.yaml` for that project's target, debugger, permissions, reports, logs, and artifact roots.

Bootstrap each project from the project directory:

```bash
aihil init
aihil doctor
aihil mcp-config > .mcp.json
```

MCP stdio command:

```text
aihil mcp-stdio --config .aihil/config.yaml
```

Project MCP discovery config:

```text
.mcp.json
```

Use MCP tools for hardware actions:

```text
aihil_debugger_info
aihil_probe_target
aihil_flash_firmware
aihil_reset_target
aihil_get_last_report
aihil_classify_last_error
```

Required workflow:

1. Build firmware.
2. Probe before flashing.
3. Flash only validated artifacts from allowed roots.
4. Read the structured result and last report.
5. Classify failures before changing code again.

Safety:

Do not request raw OpenOCD commands. Do not flash outside allowed artifact roots. Treat `permission_denied` as authoritative.
